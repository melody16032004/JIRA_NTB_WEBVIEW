<Window x:Class="JIRA_NTB_WEBVIEW.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JIRA_NTB_WEBVIEW"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="Quản lý Nhân sự" Height="820" Width="1440"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="FontFamily" Value="Segoe UI Symbol"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="0,5,0,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#444444"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border CornerRadius="10" Background="#FF2D2D30" Margin="10">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" BlurRadius="10" Opacity="0.5" Color="#000000"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Height="30" Background="#FF1E1E1E" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                    <TextBlock Text="HỆ THỐNG QUẢN LÝ NHÂN SỰ" 
                               Foreground="White" 
                               VerticalAlignment="Center" 
                               Margin="5,0,0,0"
                               FontWeight="SemiBold"/>
                </StackPanel>

                <Rectangle Grid.Column="1" Fill="Transparent"/>

                <Button x:Name="btnReopen" Grid.Column="2" Content="⏻" Style="{StaticResource TitleBarButtonStyle}" ToolTip="Đóng và mở lại ứng dụng" Click="ReopenButton_Click"/>
                <!-- Nút Refresh -->
                <Button x:Name="btnRefresh" Grid.Column="3" Content="⟳" Style="{StaticResource TitleBarButtonStyle}" ToolTip="Làm mới trang" Click="RefreshButton_Click"/>
                <Button x:Name="btnMinimize" Grid.Column="4" Content="–" Style="{StaticResource TitleBarButtonStyle}" ToolTip="Thu nhỏ màn hình" Click="MinimizeButton_Click"/>
                <Button x:Name="btnMaximize" Grid.Column="5" Content="□" Style="{StaticResource TitleBarButtonStyle}" ToolTip="Phóng to màn hình" Click="MaximizeButton_Click"/>
                <Button x:Name="btnClose" Grid.Column="6" Content="✕" Style="{StaticResource CloseButtonStyle}" ToolTip="Đóng cửa sổ" Click="CloseButton_Click"/>

            </Grid>

            <wv2:WebView2 Grid.Row="1" x:Name="webView" Margin="1,0,1,1"/>

            <Grid Grid.Row="1" x:Name="loadingIndicator" Background="#802D2D30" Visibility="Collapsed">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="100"/>
                    <TextBlock Text="Đang kết nối..." Foreground="White" Margin="0,10,0,0"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" x:Name="errorPage" Background="#FF2D2D30" Visibility="Collapsed">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" >
                    <TextBlock Text="Không thể kết nối tới máy chủ" 
                   Foreground="White" FontSize="16" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="Vui lòng kiểm tra kết nối Internet hoặc địa chỉ server." 
                   Foreground="#BBBBBB" FontSize="13" TextAlignment="Center" Margin="0,0,0,20"/>
                    <Button Content="↻ Thử lại" Width="120" Height="35" 
                Background="#007ACC" Foreground="White" FontWeight="Bold" 
                BorderThickness="0" Cursor="Hand" Click="RetryConnection_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>